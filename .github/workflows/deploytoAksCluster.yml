on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n proencoder -g proencoder8027-rg --location "East Asia" --sku Standard --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n proencoder --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n proencoder --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n proencoder --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: proencoder.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t proencoder.azurecr.io/kmac:${{ github.sha }} --label dockerfile-path=Dockerfile
        docker push proencoder.azurecr.io/kmac:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_kmac_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep proencoder | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace proencoder`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: proencoder
        container-registry-url: proencoder.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: kmacdockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: proencoder
        manifests: |
          manifests/deployment.yml
          manifests/service.yml
        images: |
          proencoder.azurecr.io/kmac:${{ github.sha }}
        imagepullsecrets: |
          kmacdockerauth   
