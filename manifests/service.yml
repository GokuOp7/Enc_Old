apiVersion: v1
kind: Service
metadata:
    name: "kmac-0fc8"
    labels:
        app: "kmac-0fc8"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kmac-0fc8"