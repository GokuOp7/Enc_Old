apiVersion: v1
kind: Service
metadata:
    name: "kmac-6243"
    labels:
        app: "kmac-6243"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kmac-6243"